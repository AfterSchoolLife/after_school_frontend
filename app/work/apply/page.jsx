'use client'
import { Card, CardContent } from "@mui/material"

const applyComponent = () => {
    return <Card className="card work-register-card">
        <CardContent>
            
        </CardContent>
    </Card>
}

export default applyComponent